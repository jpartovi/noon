# iOS App

SwiftUI iOS app for voice-based calendar interaction.

## Running

1. Open `Noon.xcodeproj` in Xcode
2. Configure the backend URL in the app configuration (or set `BACKEND_URL` environment variable)
3. Build and run the app on a simulator or device
